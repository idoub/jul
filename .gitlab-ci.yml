image: node:latest

cache:
  paths:
  - node_modules/

before_script:
  - npm install

stages:
  - build
  - test
  - deploy

build:
  stage: build
  script:
    - ./node_modules/.bin/gulp build
  artifacts:
    paths:
      - dist
  only:
    - master

test:
  stage: test
  script:
    - ./node_modules/.bin/gulp test
  artifacts:
    paths:
      - test
  only:
    - master

pages:
  stage: deploy
  script:
    - ./node_modules/.bin/gulp doc
  artifacts:
    paths:
      - public
  only:
    - master