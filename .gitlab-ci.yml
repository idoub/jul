image: node:latest

cache:
  paths:
  - node_modules/

before_script:
  - npm install

stages:
  - build
  - test
  - deploy

build:
  stage: build
  script:
    - npm run build
  artifacts:
    paths:
      - dist
  only:
    - master
  tags:
    - docker
    - node

test:
  stage: test
  script:
    - npm run test
  artifacts:
    paths:
      - test
  only:
    - master
  tags:
    - docker
    - node

pages:
  stage: deploy
  script:
    - npm run deploy
  artifacts:
    paths:
      - public
  only:
    - master
  tags:
    - docker
    - node